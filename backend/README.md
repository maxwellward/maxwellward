A good guide on setting up a virtual environment for this project is here: https://fastapi.tiangolo.com/virtual-environments/

In backend folder `source .venv/bin/activate`

Then `uvicorn app.main:app --reload`